name: Đọc tiếng lòng yêu em #Tên quy trình
on: 
  push: 
    branches: ["main"] #Chạy khi có code mới đẩy lên nhánh main
jobs:
  thuc_hien_doc_tieng_long_file:
    runs-on: ubuntu-latest   #mượn máy ảo ubuntu của github để chạy

    steps:
      - name: Bước 1 - Tìm kiếm Her
        run: echo "Tìm kiếm cô ấy"

      - name: Bước 2 - Tìm hiểu
        uses: actions/checkout@v6.0.2 

      - name: Bước 3 - Đọc tiếng lòng
        run: cat README.md
